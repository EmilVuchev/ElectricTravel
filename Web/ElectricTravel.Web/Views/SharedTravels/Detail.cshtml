@model ElectricTravel.Web.ViewModels.SharedTravels.SharedTravelDetailsViewModel
@{
    ViewData["Title"] = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<table class="table col-md-6 offset-md-3 bg-gradient-info text-dark table-striped">
        <thead>
            <tr>
                <th scope="col"></th>
                <th scope="col">"@Model.CreatedBy.FirstName"</th>
                <th scope="col">@Model.PlaceForLuggage</th>
                <th scope="col">@Model.Seats</th>
                <th scope="col">@Model.SmokeRestriction</th>
                <th scope="col">@Model.StartDestination.Name</th>
                <th scope="col">@Model.EndDestination.Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="table-active">@Model.CreatedBy.FirstName</td>
                <td class="table-active">@Model.PlaceForLuggage</td>
                <td class="table-active">@Model.Seats</td>
                <td class="table-active">@Model.SmokeRestriction</td>
                <td class="table-active">@Model.StartDestination.Name</td>
                <td class="table-active">@Model.EndDestination.Name</td>
            </tr>
        </tbody>
    </table>*@

<div class="col1 one-half">
    <h2>Trip Details</h2>
    <p>
        <label>Route:</label>
        <span class="align-content-center">
            "@Model.StartDestination.Name"<br>
            "@Model.EndDestination.Name"
        </span>
    </p>
</div>

